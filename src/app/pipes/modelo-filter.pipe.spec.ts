import { ModeloFilterPipe } from './modelo-filter.pipe';

describe('ModeloFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new ModeloFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
