import { TipoequipoFilterPipe } from './tipoequipo-filter.pipe';

describe('TipoequipoFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TipoequipoFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
